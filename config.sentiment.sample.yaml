model_name: nlpaueb/legal-bert-base-uncased
device: cuda
amp: bf16
max_length: 512
batch_size: 16
eval_batch_size: 32
lr: 2e-5
weight_decay: 0.01
num_epochs: 5
warmup_steps: 0
grad_accum_steps: 2
num_workers: 4
pin_memory: true
persistent_workers: true
prefetch_factor: 2
compile: false
gradient_checkpointing: true
log_every: 20
grad_clip: 1.0
save_dir: checkpoints_sentiment
seed: 13

data_path: data/sigmalaw_absa/train.jsonl
eval_path: data/sigmalaw_absa/dev.jsonl
